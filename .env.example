# This file is a pointer only.
# The authoritative variable contract lives in .env.sample at the repository root.
#
# To set up your environment:
#   1. Copy .env.sample to .env
#   2. Fill in the values (golden path: `azd up` then `azd env get-values > .env`)
#
# Do NOT add variables here — edit .env.sample instead so all tools stay in sync.
