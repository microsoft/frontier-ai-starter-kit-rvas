# Action Tools backend dependencies (separate from the root requirements.txt so the
# REST API + MCP server can run standalone in their own venv if desired).
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
httpx>=0.27.0
fastmcp>=3.0.0,<4
